# Zeyi Ren Academic Homepage

This is the static source for `https://zeyi0827.github.io`.

## Publish

1. Create a public GitHub repository named `zeyi0827.github.io` under the `zeyi0827` account.
2. Push this folder to the repository's `main` branch.
3. In GitHub, open `Settings -> Pages` and confirm the source is `Deploy from a branch`, `main`, `/root`.

The old homepage can stay live at `https://zeyi0827.github.io/zeyi.github.io/` until the new one is fully verified.
